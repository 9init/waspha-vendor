import 'package:flutter/material.dart';

class UpdateDriverDataBodyWidget extends StatelessWidget {
  const UpdateDriverDataBodyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
