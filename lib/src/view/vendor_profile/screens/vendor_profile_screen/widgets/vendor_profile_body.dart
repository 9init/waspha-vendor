import 'package:flutter/material.dart';

class AdminProfileBody extends StatelessWidget {
  const AdminProfileBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [

      ],
    );
  }
}
